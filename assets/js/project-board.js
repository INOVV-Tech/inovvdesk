/**
 * Project Board — interactive board behavior.
 *
 * Phase 0: load guard only. Phase 1 (MVP) adds drag & drop between lists,
 * in-list reordering and the mobile move fallback, reusing the patterns
 * from kanban.js (placeholder, ghost, revert-shake, X-CSRF-Token fetch).
 */
(function () {
    var board = document.querySelector('[data-project-board]');
    if (!board) return;
})();